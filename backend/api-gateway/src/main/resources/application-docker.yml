server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: application-user
          uri: lb://APPLICATION-USER
          predicates:
            - Path=/api/v1/application-users/**

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
