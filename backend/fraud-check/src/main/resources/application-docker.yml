server:
  port: 8082

spring:
  application:
    name: fraud-check
  datasource:
    url: jdbc:postgresql://postgres:5432/fraud-check
    username: 'postgres'
    password: '1478236950'
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: 'true'
    hibernate:
      ddl-auto: update
    show-sql: 'true'

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/